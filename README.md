# REST-API-PORTFOLIO APP
"RUN THE FOLLOWING COMMANDS TO ADD NODEMODULES & PACKAGE.LOCKJSON FILES"
npm list vite
npm install vite --save-dev
rm -rf node_modules package-lock.json
npm install
node -v
npm run build
